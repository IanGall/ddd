<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iantech.infrastructure.persistent.dao.IRbacRolePermissionDao">

    <select id="selectPermissionIdsByRoleId" resultType="java.lang.Long">
        SELECT permission_id
        FROM rbac_role_permission
        WHERE account_id = #{accountId} AND role_id = #{roleId}
        ORDER BY permission_id ASC
    </select>

    <delete id="deleteByRoleIdAndPermissionIds">
        DELETE FROM rbac_role_permission
        WHERE account_id = #{accountId} AND role_id = #{roleId}
          AND permission_id IN
        <foreach collection="permissionIds" item="permissionId" open="(" close=")" separator=",">
            #{permissionId}
        </foreach>
    </delete>

    <insert id="batchInsert">
        INSERT INTO rbac_role_permission (account_id, role_id, permission_id, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.accountId}, #{item.roleId}, #{item.permissionId}, NOW())
        </foreach>
    </insert>

    <delete id="deleteAllByRoleId">
        DELETE FROM rbac_role_permission
        WHERE account_id = #{accountId} AND role_id = #{roleId}
    </delete>

    <delete id="deleteAllByPermissionId">
        DELETE FROM rbac_role_permission
        WHERE account_id = #{accountId} AND permission_id = #{permissionId}
    </delete>

</mapper>
