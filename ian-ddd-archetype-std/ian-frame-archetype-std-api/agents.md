# API 模块协作说明

## 模块定位
- 本模块定义对外暴露的 API 契约（请求、响应、接口定义）。
- 关注协议稳定性与语义清晰度。

## 变更边界
- 允许修改：DTO、VO、接口定义、参数校验约束。
- 禁止修改：应用编排逻辑、领域规则、持久化细节。

## 依赖约束
- 仅依赖契约所需的 `ddd-common` 公共类型与必要校验能力。
- 不引入基础设施实现类，不执行业务流程。

## 提交前检查
- 接口字段命名与含义保持向后兼容。
- 删除未使用的契约字段与无效注释。
